#inlcude<stdio.h>
#include <stdio.h>

int main() {
    int a = 41, b = 32, c = 25, x = 2;
    int y;

    y = a * x * x + b * x + c;

    printf("The predicted value is %d units\n", y);

    return 0;
}
